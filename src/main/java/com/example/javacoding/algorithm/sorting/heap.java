package com.example.javacoding.algorithm.sorting;

import java.util.Arrays;

// 平均时间复杂度 O(nlogn)
public class heap {
    public void heap(int[] sourceArray) {
        int[] arr = Arrays.copyOf(sourceArray, sourceArray.length);
        // 构建 最大堆

    }
}
